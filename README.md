# saizen-modules

Optional Saizen stream module catalogs (HTTPS `index.json` + scripts).

Add this catalog URL in Saizen → Modules → Extra catalog URL:

```
https://raw.githubusercontent.com/Surajsm60720/saizen-modules/main/index.json
```

This repository must stay **public** so the app can fetch `raw.githubusercontent.com` without auth.
